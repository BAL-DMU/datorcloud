# Authoritative dependencies live in pyproject.toml. This file is kept for
# legacy tooling (docker-compose, notebooks) that still expects requirements.txt.
# Install the package directly with `pip install -e .[dagster,test]` whenever
# possible.

minio>=7.1.15
duckdb>=1.2.0
pandas>=1.5.3
python-dotenv>=1.0.0

# Dagster integration (optional, install with pip install -e .[dagster])
dagster>=1.4.0
dagster-webserver>=1.4.0

# Tests
pytest>=7.3.1
pytest-cov>=4.1.0

# Documentation
mkdocs==1.5.2
mkdocs-material==9.1.1
plantuml-markdown==3.7.0
pymdown-extensions==10.0
